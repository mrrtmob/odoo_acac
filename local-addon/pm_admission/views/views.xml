<odoo>
  <data>
    <!-- explicit list view definition -->
<!--
    <record model="ir.ui.view" id="pm_admission.list">
      <field name="name">pm_admission list</field>
      <field name="model">pm_admission.pm_admission</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
-->

    <!-- actions opening views on models -->
<!--
    <record model="ir.actions.act_window" id="pm_admission.action_window">
      <field name="name">pm_admission window</field>
      <field name="res_model">pm_admission.pm_admission</field>
      <field name="view_mode">tree,form</field>
    </record>
-->

    <!-- server action to the one above -->
<!--
    <record model="ir.actions.server" id="pm_admission.action_server">
      <field name="name">pm_admission server</field>
      <field name="model_id" ref="model_pm_admission_pm_admission"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": model._name,
        }
      </field>
    </record>
-->

    <!-- Top menu item -->
<!--
    <menuitem name="pm_admission" id="pm_admission.menu_root"/>
-->
    <!-- menu categories -->
<!--
    <menuitem name="Menu 1" id="pm_admission.menu_1" parent="pm_admission.menu_root"/>
    <menuitem name="Menu 2" id="pm_admission.menu_2" parent="pm_admission.menu_root"/>
-->
    <!-- actions -->
<!--
    <menuitem name="List" id="pm_admission.menu_1_list" parent="pm_admission.menu_1"
              action="pm_admission.action_window"/>
    <menuitem name="Server to list" id="pm_admission" parent="pm_admission.menu_2"
              action="pm_admission.action_server"/>
-->
  </data>
</odoo>